REACT_APP_API_URL=https://billing-backend-dfm9.onrender.com
# In production the frontend will use the above API base URL.
# During development CRA proxy (package.json) will route /api to http://localhost:5000 so this file is not necessary for local dev.
